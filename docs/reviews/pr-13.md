# Review: PR #13 — Port assessment and Windows port plan

Reviewed at commit `4572093` (branch `docs/port-docs` into `main`). Docs-only PR: two new design files, 489 lines, no code changes.

Grounding: `gh pr diff 13`, full read of both added docs, spot-check of 12 cited file:line anchors against `/home/andy/Documents/GitHub/tern` at `4572093`.

## Scope check

**CLEAN.** Intent matches delivery: feasibility inventory plus phased Windows plan, no implementation.

## Verdict

**APPROVE** (docs-only, informational follow-ups optional before or after merge).

**Blocking: 0. Should-fix: 2. Follow-up: 3.**

**safe_to_merge: true**

These are planning documents. Nothing here ships runtime behavior. The inventory matches the repo closely enough to guide a port. Fix the line-reference and MVP wording nits when convenient; they do not block merge.

---

## File:line spot-check (12 anchors)

| Citation | Result | Evidence |
|---|---|---|
| `Controller.cpp:817-837` (`ensurePickerEngine`) | OK | `src/app/Controller.cpp:817-837` loads Picker QML and calls `configureLayerShell` |
| `Controller.cpp:860-883` (`configureLayerShell`) | OK | `src/app/Controller.cpp:860-883` sets layer-shell anchors, exclusive keyboard, scope |
| `discovery.cpp:880-887` (`defaultDiscoveryPaths`) | OK | `src/core/discovery.cpp:880-887` |
| `main.cpp:143-151` (`KDBusService`) | OK | `src/app/main.cpp:143-151` |
| `launcher.cpp:245-267` (activation token env) | OK | `src/core/launcher.cpp:254-267` sets/restores `XDG_ACTIVATION_TOKEN` around spawn |
| `Picker.qml:6-20` | OK | Layer-shell import and overlay properties at lines 6-20 |
| `Hold.qml:6-20` | OK | Same pattern, scope `lane-hold` |
| `main.cpp:67` (`KCrash`) | OK | `KCrash::initialize()` at line 67 |
| `Controller.cpp:472-482` (`makeDefaultBrowser`) | OK | xdg-mime + xdg-settings calls |
| `Controller.cpp:340-401` (`openUrl` + token capture) | OK | Token capture 342-352; re-entrancy queue 360-362 |
| `CMakeLists.txt:43-86` | OK | KF6/LayerShell deps and Linux install rules |
| `Controller.cpp:748-754` (notifications) | **Wrong** | Lines 748-754 are recent-target bookkeeping and `toast()` call site; `KNotification` bodies are at 685-690, 796-804, 807-814 |

Additional notes on other citations (not in the 12-spot sample):

- `SourceInfo.cpp:6-11` is correct for line numbers but the file lives at `src/app/SourceInfo.cpp`, not repo root.
- `config.cpp:160` is `defaultConfigPath()`, not a generic "config module" pointer; still a valid free-surface cite.
- Zero `#ifdef Q_OS_*` claim verified: no matches under `src/`, `CMakeLists.txt`, or tests.

---

## Platform interface vs assessment inventory

The proposed `Platform` API and call-site table cover the main seams: overlay, single-instance IPC, default browser, autostart, tray, notifications, activation handoff, discovery roots, and email action targets.

Surfaces in the assessment inventory **not** mapped in the plan's call-site table:

| Inventory surface | Gap | Severity |
|---|---|---|
| Crash handler (`main.cpp:67`, `KCrash`) | P0 says "drop or no-op on Windows" but no `Platform` hook or table row | Informational — `#ifdef` in `main.cpp` is enough |
| Browser profile paths (`discovery.cpp:297-384`, `fingerprint()` tables) | Only `defaultDiscoveryPaths()` (880-887) is in the table; per-OS profile tables appear in P1 work items but not under Platform mapping | Should-fix — add a row or cross-ref so fingerprint tables are not lost |
| Layer-shell fallback warning (`Controller.cpp:862-865`) | Implied inside Linux `configureOverlay`; not named | OK as-is |
| Flatpak / container registry / build-install rows | Correctly treated as `#ifdef`, installer, or CMake, not `Platform` | OK |

No inventory row describes behavior that has no plan path. The fingerprint-table omission is the only real coverage gap in the abstraction map.

---

## Internal consistency

**Effort numbers:** Consistent. Assessment Windows total 10-14 pw (+3-5 tail). Plan phases sum to 9.5-13 pw (P0 3-4 + P1 2.5-3.5 + P2 2-3 + P3 1.5-2.5). Ranges align.

**Phase demos vs work items:** Mostly aligned. P0 demo (default handler + IPC + minimal picker) matches P0 tasks. P1 demo (rules, remembered, discovery, containers) matches P1 tasks.

**MVP wording tension (should-fix):**

- Section 5 lists **hold HUD (P2)** as explicitly post-MVP for launch.
- Closing **MVP (2 lines)** says hold HUD and containers can land in P1/P2 without blocking first release.

Pick one story: either hold is launch-blocking for MVP (section 5) or it is an acceptable fast-follow (line 320). Containers already have a clear P1 home; hold does not.

**Strategic framing:** Assessment recommends "Do not port"; Windows plan assumes a port anyway. That is fine if read as assessment-then-execution-plan, but link the two explicitly in the plan intro so readers do not think the assessment was overturned silently.

---

## Voice and style

- No em dashes in either new doc.
- No AI vocabulary hits (`leverage`, `robust`, `seamless`, `delve`, `crucial`, etc.) in `port-assessment.md` or `windows-port-plan.md`.
- `[verified]` / `[inference]` labeling is used consistently.

---

## Should-fix (non-blocking)

1. **`docs/designs/port-assessment.md` and `windows-port-plan.md` — notifications cite `Controller.cpp:748-754`**
   - Replace with `796-804` (`toast`) or list all three KNotification sites: `685-690`, `796-804`, `807-814`.

2. **`windows-port-plan.md` — MVP vs hold HUD**
   - Reconcile section 5 "Explicitly post-MVP" with the closing MVP paragraph (line 320).

---

## Follow-up (informational)

1. Add `src/app/` prefix wherever `SourceInfo.cpp` is cited.
2. Add a Platform call-site row (or P1 cross-ref) for `discovery.cpp:297-384` fingerprint path tables.
3. One sentence in `windows-port-plan.md` intro: "Assessment recommends deferring ports; this plan is the Windows path if that decision changes."

---

## Pre-landing checklist (docs-only pass)

Pass 1 critical categories (SQL, races, LLM trust, shell injection, enums): N/A — no code.

Pass 2: one documentation accuracy finding (notification line cite). No CI, security, or test gaps introduced.

Specialists / Review Army / adversarial passes: skipped (docs-only, per lighter review scope).

---

## gstack

- Session: `333711-1789604098-20a0af21`
- Branch slug: `docs-port-docs`
- Quality score: 8.5 / 10 (accurate inventory, minor cite and MVP wording gaps)
