# Review: PR #15 — Fix doc drift (hold default, PKGBUILD, flatpak)

Reviewed at commit `fa6e3e6` (branch `fix/doc-drift` into `main`).
Scope: three markdown files (`AGENTS.md`, `DESIGN.md`, `README.md`). Docs-only;
no Lane code, build, or daemon interaction. Spot-checked load-bearing claims
against the `fix/doc-drift` checkout.

## Scope check

**CLEAN.** Intent: fix hold-default doc drift, document PKGBUILD install path,
note flatpak profile discovery. Delivered: exactly that in three files (+25/-16).

## Verdict

**APPROVE.** All factual claims in the diff match code on this branch. The PR
closes the hold-default drift flagged in `docs/reviews/ceo.md` round 2 and the
synthesis P1 list from PR #14. Safe to merge as-is.

**Blocking: 0. Should-fix: 0. Informational: 1.**

`safe_to_merge`: **true**

## Spot-check: load-bearing claims

| Claim | Source in diff | Verified |
|-------|----------------|----------|
| `holdAutoOpen` defaults to `false` | `AGENTS.md` example + prose; `DESIGN.md`; `README.md` | **Yes.** `src/core/types.h:149` (`bool holdAutoOpen = false`), `src/core/config.cpp:261` (`toBool(false)`), `docs/config.schema.json:65-68` (`"default": false`), `tests/test_destination.cpp:213-215` (default-off regression). |
| Hold bar is opt-in via Preferences | `DESIGN.md:45`, `README.md:17-18,49-51` | **Yes.** `src/qml/pages/PreferencesPage.qml:47-52` — `FormSwitchDelegate` "Pause before opening" bound to `controller.holdAutoOpen`. |
| Flatpak browsers discovered; profiles under `~/.var/app/<app-id>/` | `README.md:33-34`, `AGENTS.md:28-30` | **Yes.** `src/core/discovery.cpp:250-310` prepends `paths.home + "/.var/app/" + flatpakId + "/" + flatpakRelDir` for Gecko; `:317-323` sets Chromium `dataDir` under the same prefix. PR #11 ("Discover Flatpak-packaged browsers correctly") is merged on `main`. |
| System install via `packaging/PKGBUILD`; `makepkg -si` | `README.md:171-172` | **Yes.** `packaging/PKGBUILD` exists; `cd packaging && makepkg -si` is the standard Arch build-and-install invocation for a local PKGBUILD. "AUR package is planned" matches ceo review state (no AUR package yet). |

## Pre-landing checklist (docs-only pass)

Pass 1 (SQL, races, LLM trust, shell injection, enum completeness): **N/A** — no code.

Pass 2: **No issues found.**

## Voice and style

- **Em dashes:** One pre-existing instance in touched file `DESIGN.md:49`
  (`never held — the user wrote the rule`). Not introduced by this PR (same
  punctuation in the deleted hunk). New prose uses hyphens or plain sentences.
- **AI vocabulary:** None in the diff.
- **Diff hygiene:** Only the three documented files changed; nothing else
  slipped in.

## Informational

1. **AGENTS.md hold section omits Preferences pointer (confidence: 7/10).**
   `README.md` and `DESIGN.md` tell readers to enable the hold bar in
   Preferences; `AGENTS.md:80-84` only documents the JSON key
   (`true (default false)`). Accurate for config, but agents directed at
   AGENTS.md may miss the UI toggle in `PreferencesPage.qml`. Optional
   one-line add; not merge-blocking.

## gstack review summary

```
Pre-Landing Review: 1 issue (0 critical, 1 informational)

Scope Check: CLEAN
Intent: Fix hold-default, PKGBUILD, and flatpak doc drift
Delivered: Three-file doc correction matching code and merged PR #11
```
