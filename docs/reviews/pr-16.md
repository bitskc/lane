# Review: PR #16 — Fix round-2 gstack review misc findings

**Branch:** `fix/misc-review` → `main`  
**Commit:** `43110c2`  
**Worktree:** `/tmp/lane-misc-fixes`

## Scope check

**DRIFT DETECTED (contamination).**

| | |
|---|---|
| **Intent** | Changelog drift fixes, update-checker GitHub host pinning + 403 narrowing, CI Release build, appstreamtest install-before-ctest docs/PKGBUILD, `--rediscover` doc honesty |
| **Delivered** | All of the above in code/docs **plus** the entire PR #18 controller-races CHANGELOG block and AGENTS.md config-key removals, with **no** matching `Controller.cpp` / `config.cpp` / schema changes |

**Out-of-scope / cross-PR hunks (belong to PR #18):**

- `CHANGELOG.md:21-43` — six `### Fixed` bullets (launch race, stale picker, `recentTargetIds`/`toastMs`, argv[0], pending URL cap, `holdMs` clamp). Byte-identical to PR #18's diff; no implementation in this PR.
- `AGENTS.md:52-53,61` — removes `toastMs` and `recentTargetIds` from the example config. PR #18 removes the real keys from schema/code; this PR only edits the sample JSON.

## Verdict

**REQUEST CHANGES.** The update-checker, CI, PKGBUILD, and doc-honesty work is sound. Do **not** merge until the PR #18 CHANGELOG/AGENTS contamination is stripped or PR #18 lands first and this branch is rebased to drop the duplicate hunks.

**Blocking: 1 (contamination). Should-fix: 0. Informational: 2.**

**safe_to_merge: false**

## Contamination assessment

Confirmed: sibling worktree collision absorbed PR #18 prose without PR #18 code.

- `gh pr diff 18` and `gh pr diff 16` share the same `CHANGELOG.md` `### Fixed` section (lines 21-43).
- PR #18's file list includes `Controller.cpp`, `config.cpp`, `docs/config.schema.json`; PR #16's does not.
- If **#16 merges first**: changelog claims six controller/config fixes that are not shipped; #18 will hit a messy CHANGELOG conflict.
- If **#18 merges first**: rebase #16 and drop the duplicate `### Fixed` block and AGENTS.md key removals (keep only #16-specific changelog edits and rediscover docs).

## Findings

### Blocking

- **[P1] (confidence: 10/10) `CHANGELOG.md:21-43` — PR #18 changelog landed without PR #18 code**  
  Quoted hunk opens `### Fixed` and documents launch-race, picker, dead keys, argv[0], queue cap, and `holdMs` fixes. Grep of this PR's diff: zero changes under `src/app/Controller.cpp`, `src/core/config.cpp`, or `docs/config.schema.json`.  
  **Fix:** Delete lines 21-43 from this PR; let PR #18 own that section.

- **[P1] (confidence: 10/10) `AGENTS.md:52-53,61` — example config keys removed ahead of PR #18**  
  Removes `toastMs` and `recentTargetIds` from the sample JSON while `docs/config.schema.json` on this branch still defines both keys.  
  **Fix:** Revert those two deletions here, or merge only after PR #18 lands and rebase.

### In-scope work (verified)

- **[OK] (confidence: 9/10) Host pinning — `src/core/updatedecision.cpp:13-23`, `src/app/UpdateChecker.cpp:51-54`, `95-99`**  
  `isAllowedGitHubUpdateHost` allows only `github.com` and `api.github.com` (case-insensitive). Applied to initial request URL, manual redirect targets, and `html_url` from the JSON body.  
  API entry point is `api.github.com` (`repoinfo.h:18-21`). GitHub release **asset** redirects to `objects.githubusercontent.com` are irrelevant: this checker never downloads assets, only reads JSON and opens `html_url` in the browser. Pinning redirects to GitHub hosts is correct for this threat model.

- **[OK] (confidence: 9/10) 403 narrowing — `src/core/updatedecision.cpp:41-57`, `tests/test_updatechecker.cpp:64-80`**  
  `429` → rate-limited message. `403` with parseable `x-ratelimit-reset > 0` → rate-limited (GitHub sometimes uses 403 for limits). Bare `403` → `describeUnexpectedStatus` ("HTTP 403"), not rate limiting. Tests discriminate all three paths.

- **[OK] (confidence: 9/10) CI Release build — `.github/workflows/ci.yml:29`**  
  `CMAKE_BUILD_TYPE=Release` matches README/RELEASING and production packaging. No `NDEBUG`/`Debug`-only assumptions in tests or sources (grep over tree). Existing install-to-`build/throwaway-install` step before `ctest` unchanged except build type.

- **[OK] (confidence: 9/10) PKGBUILD `check()` — `packaging/PKGBUILD:27-28`**  
  Uses `cmake --install build --prefix build/throwaway-install` (real prefix install, not `DESTDIR`), mirroring CI comments. `package()` still does `DESTDIR="$pkgdir" cmake --install build`; throwaway tree stays under `$srcdir/build/` and is not packaged.

- **[OK] (confidence: 9/10) Changelog drift corrections (legitimate to this PR)**  
  - `CHANGELOG.md:66-67` — "Dead remembered entries are also pruned when config reloads" matches `Controller.cpp:659-668` (`clearDeadRemembered()` in `reload()`).  
  - `CHANGELOG.md:317-320` — rules `title`/`process` compatibility note matches `matcher.cpp:37-44` (warns, cannot match on Wayland).  
  - No em dashes in new prose. Human voice reads clean.

- **[OK] (confidence: 9/10) `--rediscover` doc honesty — `README.md:140-144`, `AGENTS.md:25-31,207,221-225`**  
  Correctly states D-Bus handoff when daemon is up, full resident process when not. Matches actual CLI behavior described elsewhere in AGENTS.md.

### Informational

- **[P3] (confidence: 7/10) Merge order** — Land PR #18 before #16, or strip shared hunks from #16 before either merges, to avoid changelog lying about shipped fixes.

- **[P3] (confidence: 8/10) `docs/reviews/eng.md:87`** — Still says CI builds Debug; this PR fixes `ci.yml` but not that review artifact. Non-blocking stale doc in reviews folder.

## Critical pass (checklist)

Pre-Landing Review: 1 issue (1 critical, 0 informational after in-scope verification)

**NEEDS INPUT:**

- `CHANGELOG.md:21-43` — PR #18 changelog contamination without code  
  Recommended fix: remove the `### Fixed` block from this PR; rebase after #18 if needed

No SQL, race, injection, or enum issues in the in-scope code changes.

## Quality

| Area | Score | Note |
|------|-------|------|
| Update checker security | 9/10 | Host pin + redirect vet + 403 split are tight |
| CI/PKGBUILD | 9/10 | Release parity; install-before-ctest pattern correct |
| Docs (in-scope) | 8/10 | Rediscover and appstreamtest honesty good |
| Scope hygiene | 3/10 | PR #18 prose without code is a merge blocker |

**Overall quality_score: 6.5** (would be ~8.5 without contamination)

## Test evidence

PR description claims `ctest` 12/12 after throwaway install. Tests for new behavior: `tests/test_updatechecker.cpp:64-80,104-109,122-140`. Not re-run in this review session (assignment: no Lane daemon/windows).

---

## Round 2 (re-review)

**Commits:** `fa3df89` (strip PR #18 hunks) + `5325fdc` (restore truncated 0.1.0)  
**HEAD:** `5325fdc`  
**Re-reviewed:** 2026-09-17

### Scope check

**CLEAN.** Intent matches delivery. No PR #18 contamination remains.

| | |
|---|---|
| **Intent** | Changelog drift fixes, update-checker host pinning + 403 narrowing, CI Release build, appstreamtest install-before-ctest docs/PKGBUILD, `--rediscover` doc honesty |
| **Delivered** | Exactly that. 11 files, +99/-27. No `Controller.cpp`, `config.cpp`, or `config.schema.json` changes. |

### Contamination verification

| Check | Result |
|-------|--------|
| `CHANGELOG.md` lacks `### Fixed` PR #18 block | **Pass** — grep for `recentTargetIds`, `toastMs`, `activation token`, `pending URL` returns zero hits |
| `AGENTS.md` example config still has `toastMs` and `recentTargetIds` | **Pass** — `AGENTS.md:53,66` both keys present |
| PR diff file list matches intent | **Pass** — same 11 files as round-1 in-scope work; no controller/schema files |

### CHANGELOG 0.1.0 restoration

Compared `git show origin/main:CHANGELOG.md` vs `HEAD` for the `## [0.1.0]` section:

- **Fully restored.** Every bullet from main is present except the one intentional drift correction (rules engine `title`/`process` note at lines 293-296).
- The accidental truncation from `fa3df89` cleanup is fixed in `5325fdc`: Discovery, pipeline, CLI, tray, and footer link bullets all match main.
- Total line count: main 326, HEAD 330 (+4 from expanded rules-engine bullet only).
- `CHANGELOG.md` diff vs main is exactly two hunks: remembered-entry pruning (`0.2.0` section) and rules-engine honesty (`0.1.0` section). No other deltas.

### In-scope work (re-verified, unchanged from round 1)

All round-1 **[OK]** findings still hold on `5325fdc`:

- Host pinning (`updatedecision.cpp:13-23`, `UpdateChecker.cpp:51-54`)
- 403 narrowing (`updatedecision.cpp:48-57`, `test_updatechecker.cpp:64-80`)
- CI Release build (`.github/workflows/ci.yml:29`)
- PKGBUILD throwaway install (`packaging/PKGBUILD:27-28`)
- Changelog drift corrections match code (`Controller.cpp:659-668`, `matcher.cpp:37-44`)
- `--rediscover` doc honesty (`README.md`, `AGENTS.md`)

### Round 2 verdict

**APPROVE.** Contamination removed cleanly. CHANGELOG 0.1.0 section fully restored with only the intended rules-engine correction. All in-scope code and docs from round 1 intact.

**Blocking: 0. Should-fix: 0. Informational: 1.**

**safe_to_merge: true**

### Informational (unchanged)

- **[P3] `docs/reviews/eng.md:87`** — Still says CI builds Debug; this PR fixes `ci.yml` but not that review artifact. Non-blocking.

### Quality (round 2)

| Area | Score | Note |
|------|-------|------|
| Update checker security | 9/10 | Unchanged |
| CI/PKGBUILD | 9/10 | Unchanged |
| Docs (in-scope) | 8/10 | Unchanged |
| Scope hygiene | 9/10 | Contamination stripped; cleanup commits are surgical |

**Overall quality_score: 8.5**
