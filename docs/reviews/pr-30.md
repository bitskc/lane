# Review: PR #30 — Fix changelog corruption and stale docs

Reviewed at commit `3fde33f` (branch `fix/docs-wave3` into `main`).
Method: gstack `/review` checklist. Sources read via `git show
fix/docs-wave3:<file>` and `gh pr diff 30` (no branch switch, no Lane
run).

## Scope check

**CLEAN.** Intent: repair committed CHANGELOG corruption (conflict-marker
tool output, truncated Security tail, deleted 0.1.0 section), re-add six
PR #18 Fixed entries, drop dead `toastMs`/`recentTargetIds` from
AGENTS.md, close issues #29, #20, #22, #27. Delivered: two files,
79 insertions / 24 deletions, docs-only, matching PR body.

Pre-Landing Review: 1 issue (0 critical, 1 informational)

## Verdict

**APPROVE.** The corruption on `main` is fully repaired. Conflict junk
is gone, the truncated Security bullet is restored, the 0.1.0 section
and footer link definitions match the last good tree, and the six PR
#18 changelog entries are verbatim. AGENTS.md example JSON is valid
with the dead keys removed.

**Blocking: 0. Should-fix: 0. Informational: 1.**

`safe_to_merge`: **true**

## Corruption repair

| Check | Result |
|-------|--------|
| Conflict-marker / tool output removed | **Yes.** `main:CHANGELOG.md` still contains `unresolved conflict detected`, `<<< ours`, and omp conflict instructions (tail ~L298+). `fix/docs-wave3:CHANGELOG.md` has zero matches for `<<<<<<`, `>>>>>>`, `=======`, `unresolved conflict`, `@ours`, `@theirs`. |
| Truncated Security bullet restored | **Yes.** Corruption on `main` cut off mid-sentence at ``instead of being``. Fix completes: ``silently replaced by defaults. Rules and remembered destinations are never discarded without a copy.`` (`CHANGELOG.md` ~L315-318 on fix branch). |
| 0.1.0 section present | **Yes.** `## [0.1.0] - 2026-09-11` through `### Security` block restored (`CHANGELOG.md` ~L320-375). Byte-identical to `59bc14f:CHANGELOG.md` from that header through EOF (verified diff). |
| Footer link definitions | **Yes.** All three present with correct URLs: `[Unreleased]: .../compare/v0.2.0...HEAD`, `[0.2.0]: .../compare/v0.1.0...v0.2.0`, `[0.1.0]: .../releases/tag/v0.1.0` (`CHANGELOG.md` ~L376-378). |
| Conflict sides merged, not dropped | **Yes.** Both pre-corruption intents survive in Unreleased/Fixed: native browser Exec= flag leak fix (`CHANGELOG.md` ~L45-49) and PR #17 layer-shell / default-button entries (`CHANGELOG.md` ~L63-66). |

## PR #18 changelog entries (six re-added)

Compared first six `### Fixed` bullets on `fix/docs-wave3:CHANGELOG.md`
against `cf9881a:CHANGELOG.md`. All six match exactly (wording,
backticks, line breaks). Entries cover: async URL snapshot, stale picker
on queued click, dead config keys, D-Bus argv[0] strip, pendingUrls cap
of 4, unified 400-5000 ms hold range.

No duplicate bullets detected across the full file.

## AGENTS.md

| Check | Result |
|-------|--------|
| `toastMs` removed | **Yes.** Diff deletes line; zero refs on fix branch. |
| `recentTargetIds` removed | **Yes.** Diff deletes line; zero refs on fix branch. |
| JSON example still valid | **Yes.** First ` ```json ` block parses with `python3 -m json.tool`. |
| Stale LayerShell.Window QML refs | **None** on fix branch. |

## Findings

### Informational

- **[INFORMATIONAL] (confidence: 10/10) CHANGELOG.md:378 — missing trailing newline**
  File ends with `[0.1.0]: https://github.com/bitskc/lane/releases/tag/v0.1.0` with no final `\n`. `gh pr diff` flags `\ No newline at end of file`. Harmless for readers; add one newline before merge if you care about POSIX text-file hygiene.

## Pass 1 categories (CRITICAL)

SQL, race conditions, LLM trust boundary, shell injection, enum
completeness: **N/A** (docs-only diff).

## Specialists

Skipped (two-file markdown diff; no code paths touched).

## Issue closure mapping

| Issue | Claim | Verified |
|-------|-------|----------|
| #29 | CHANGELOG corruption | **Fixed** — conflict junk and truncation gone; 0.1.0 + footers restored. |
| #20 | Dead `toastMs`/`recentTargetIds` in AGENTS.md | **Fixed** — keys removed from example; no other doc refs. |
| #22 | Six PR #18 entries missing from CHANGELOG | **Fixed** — verbatim restore at top of Unreleased/Fixed. |
| #27 | (docs hygiene related) | **Addressed** by the above repairs; no new doc damage introduced. |
