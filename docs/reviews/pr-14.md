# Review: PR #14 — Round-2 gstack review suite (docs only)

Reviewed at commit `9bc75a3` (branch `docs/review-round2` into `main`).
Scope: six markdown files (`docs/reviews/{eng,ceo,design,devex}.md`,
`docs/designs/gstack-full-analysis.md`, `docs/designs/lane-office-hours.md`).
No Lane code, build, or daemon interaction. Spot-checked eight load-bearing
file:line cites against the `main` checkout the reviews describe.

## Verdict

**APPROVE.** The round-2 suite is substantively accurate, internally
consistent, and safe to merge. The synthesis fix list matches what the four
reviews actually found. Residual issues are editorial (em dashes in
`eng.md` finding headers) and one low-priority synthesis omission.

**Blocking: 0. Should-fix: 1 (voice). Informational: 2.**

`safe_to_merge`: **true**

## Spot-check: eight load-bearing cites

| Cite | Claim | Verified |
|------|-------|----------|
| `Controller.cpp:360-364` | `m_inOpenUrl` queues re-entrant `openUrl` into `m_pendingUrls` | **Yes.** `src/app/Controller.cpp:360-364` sets the guard and appends `{url, forcePicker, token}`; queue drains at 389-400. |
| `Controller.cpp:768-794` | `requestActivationAndLaunch` `finish` captures `target` but `launch()` reads `m_click.openUrl` at fire time | **Yes.** `finish` lambda at 779-786 calls `launch(target, reason, token)`; `launch()` uses `m_click.openUrl` at 748. Wrong-destination race is real. |
| `Controller.cpp:862-866` | `configureLayerShell()` null check on non-wlroots | **Yes.** `LayerShellQt::Window::get` null branch with `qWarning` and early return. |
| `launcher.cpp:57-75` | Interpreter blocklist includes `env`, `flatpak-spawn`; `flatpak` itself is not blocked | **Yes.** `blockedInterpreters()` lists `env` (57) and `flatpak-spawn` (70); `flatpak` absent. Supports the `--command=` bypass claim. |
| `Picker.qml:186-187` | Filter with zero matches still sizes list to one row (`Math.max(1, count)`) | **Yes.** Height formula uses `Math.max(1, count) * rowHeight`; no empty-state label in the file. |
| `types.h:149` | `holdAutoOpen` defaults to `false` | **Yes.** `bool holdAutoOpen = false;` |
| `config.cpp:261` | Config loader defaults `holdAutoOpen` to `false` | **Yes.** `toBool(false)`. |
| `main.cpp:98-106` | `--configure` and `--rediscover` registered in `QCommandLineParser` | **Yes.** `settingsOpt` (configure alias) and `rediscoverOpt` added and registered. |

All eight cites resolve to real code at the stated lines (paths omit the
`src/app/` or `src/core/` prefix but are unambiguous in context).

## Synthesis audit (`gstack-full-analysis.md`)

**Verdicts table vs individual reviews:** Matches. Eng SHIP, CEO AUR
blocker, design "ship for daily use / fix before strangers", devex "good
enough / docs drift" all quoted faithfully in the table.

**Round-2 fix verification table (23 rows):** Spot-checked rows 1, 4, 11,
12 against source. Statuses and evidence hold. Row 12 (`clearDeadRemembered`
at `Controller.cpp:668` after `setRules()`) confirmed in `reload()`.

**Fix list vs reviewer findings:**

| Priority | Synthesis items | Traceability |
|----------|-----------------|--------------|
| P0 | None | Matches eng verdict (no ship-blockers). |
| P1 (5) | AUR publish, hold-default doc drift, stale screenshots, issue #12, flatpak/AUR docs | All appear in ceo #1, design #3/#4, devex #1/#3/#4, ceo #5. No invented P1s. |
| P2 (6) | Picker empty state, `Exec=env` dead targets, `dragId` cancel, appstreamtest, section-header height, `holdMs` clamp | eng #3 flip side, eng #4, design #1/#2, devex #2/#5. |
| P3 (12) | Launch race, stale picker, flatpak `--command=`, dead config fields, changelog drift, `handleArgs`, update URL pinning, discovery dead check, `execPrefix` quotes, pending URL cap, design/devex lows | eng #1-10 and design #6-12 / devex #6-8 batch. |

**Dropped findings:** Design should-fix #5 (Always/remembered regret path
still a settings dig) is mentioned in the design review but not given its
own synthesis row. It is partially covered by design follow-up bucket #22
("batch with next settings/picker pass"). Not a P0/P1 drop; acceptable
prioritization.

**Contradictions section:** Correctly adjudicates eng P3s as non-blocking,
picker empty state as 0.2.1, and flatpak discovery as not violating the
no-features rule. Aligns with ceo #4.

## Internal consistency

- Four reviews + synthesis; openspec lens correctly dropped (archived in
  round 1, cited in verification row 10).
- Round-1 verification tables in ceo/design/devex match synthesis rows
  where they overlap.
- Priority labels in eng (P3/P4) are preserved, not upgraded, in synthesis.
- Issue #12 reference in synthesis P1 #4 correctly cites ceo finding #5
  (verified via `gh issue view 12`).

## Voice and style

- **Em dashes:** Nine instances, all in `docs/reviews/eng.md` new-finding
  headers (`**N. [P3] Controller.cpp:768-794 — ...**`). gstack voice rule
  says no em dashes. Other five files are clean. Replace with a colon or
  period before merge if you want strict voice compliance; not a factual
  accuracy issue.
- **AI vocabulary:** None found across the six files.
- Tone reads human and specific throughout.

## Method

1. `gstack-skill-start --skill review` (SESSION_ID `777869-1789666151-baaef7cc`, TEL_START `1789666151`).
2. Read `review/SKILL.md` and `checklist.md`; docs-only lighter pass (no Review Army, no code auto-fix).
3. `gh pr view 14` / `gh pr diff 14` for scope.
4. Spot-checked eight cites in `src/app/Controller.cpp`, `src/core/{launcher,types,config}.cpp`, `src/qml/Picker.qml`, `src/app/main.cpp`.
5. Cross-walked synthesis fix list against all four review finding sections.
